{% extends 'base.html.twig' %}

{% block body %}
    <h1>Bike</h1>

    <table>
        <tbody>
            <tr>
                <th>Guid</th>
                <td>{{ bike.guid }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ bike.category }}</td>
            </tr>
            <tr>
                <th>Brand</th>
                <td>{{ bike.brand }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{% if bike.year %}{{ bike.year|date('Y-m-d') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Size</th>
                <td>{{ bike.size }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ bike.color }}</td>
            </tr>
            <tr>
                <th>Gears</th>
                <td>{{ bike.gears }}</td>
            </tr>
            <tr>
                <th>Brakes</th>
                <td>{{ bike.brakes }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('bike_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('bike_edit', { 'guid': bike.guid }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
